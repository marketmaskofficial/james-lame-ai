//@version=6
indicator("Repaint Documented Safe Idiom Test", overlay=true)
htf = input.timeframe("1W", "Higher Timeframe")
htfClose = request.security(syminfo.tickerid, htf, close[1], lookahead=barmerge.lookahead_on)
plot(htfClose, "HTF Close (confirmed via [1] + lookahead_on)", color=color.blue)
